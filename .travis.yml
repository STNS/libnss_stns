language: go
sudo: true
services:
  - docker
before_install:
- sudo apt-get -qq update
- sudo apt-get install -y libpam0g-dev
- bundle install
- mkdir docker/tmp
script:
- bundle exec rake make
go:
  - 1.6
